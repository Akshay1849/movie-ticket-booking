export default function AdminPlaceholderPage({ title }) {
  return <section className="admin-placeholder"><span className="eyebrow">Backstage module</span><h2>{title}</h2><p>This management view is queued for the next phase. Dashboard reporting is available now.</p></section>;
}
